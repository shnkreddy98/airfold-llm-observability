name: test_sql
nodes:
  - load:
      sql: |
        SELECT
          AVG(CASE 
                WHEN JSONLength(toString(error))=0 
                THEN 0 
                ELSE 1 
              END) AS error_rate
        FROM llm_json
publish: test_sql
